specVersion: "v1beta"
serviceId: "task-management-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "task-management-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
